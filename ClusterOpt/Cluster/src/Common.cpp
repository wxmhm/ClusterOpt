#include "../include/Common.h"

// Define static members (these were only declared in the header)
std::map<std::string, double> ElementInfo::atomicMasses;
std::map<std::string, int> ElementInfo::atomicNumbers;
